apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: nubrik
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: nubrik-backend:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3001
        envFrom:
        - configMapRef:
            name: nubrik-config
        - secretRef:
            name: nubrik-secret